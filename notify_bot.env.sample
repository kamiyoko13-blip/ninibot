# Sample environment file for notify_bot systemd unit
# Place a real one at /etc/notify_project/notify_bot.env (root-owned, 600)
# Format: KEY=value
#
# This file is a systemd-oriented sample. Copy values from the project-level
# `.env.example` and place the real file at `/etc/notify_project/notify_bot.env`
# with owner root:root and mode 600.

# Exchange / API
API_KEY=""
SECRET_KEY=""
EXCHANGE=bitbank

# Initial funds and periodic deposit (numbers are in JPY)
INITIAL_FUND=20000
DEPOSIT_AMOUNT=0

# SMTP (required for notifications)
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_HOST=""
SMTP_PORT=465

# Notifications
EMAIL_TO=""
# Optional: SUBJECT for email notifications
SUBJECT="通知"

# Operational flags
DRY_RUN=0
